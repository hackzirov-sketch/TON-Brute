services:
  - type: web
    name: tonfinder
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: USER_ID
        sync: false
      - key: API_KEY
        sync: false
      - key: NOTIFY_ENABLED
        value: "true"
